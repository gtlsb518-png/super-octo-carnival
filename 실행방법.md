# 🚀 실행 방법 (이것부터 읽으세요)

## 1. 파이썬 설치 확인

명령 프롬프트(cmd)에서:
```
python --version
```
버전이 안 나오면 https://www.python.org 에서 설치하세요.
**설치할 때 "Add Python to PATH" 체크 필수입니다.**

## 2. 압축 풀기

이 압축파일의 모든 파일을 **한 폴더에** 풀어주세요.
예: `C:\바이낸스봇\`

파일들이 같은 폴더에 있어야 합니다. 하위 폴더로 나누면 안 됩니다.

## 3. API 키 입력

`1_config.py` 를 메모장으로 열어서 두 줄을 고칩니다.

```python
API_KEY = "여기에_키_붙여넣기"
API_SECRET = "여기에_시크릿_붙여넣기"
TESTNET = True
```

### ⚠️ 키와 TESTNET은 반드시 '짝'으로 맞추세요

| 하려는 것 | API 키 | TESTNET |
|---|---|---|
| 테스트넷(모의거래) | 테스트넷 키 | `True` |
| 메인넷(실거래) | 메인넷 키 | `False` |

**키만 바꾸고 TESTNET을 안 바꾸면 실거래가 안 됩니다.**
키 종류로 자동 판별하지 않습니다. `TESTNET` 값이 접속 서버를 정합니다.

- 테스트넷 키 발급: https://testnet.binancefuture.com
- 메인넷 키 발급: 바이낸스 → API 관리 → **선물 거래 권한 켜기**

## 4. 실행

같은 폴더에서:
```
python 9_main.py
```

필요한 라이브러리(pandas, numpy, requests, openpyxl)는 자동 설치됩니다.
처음 실행은 설치 때문에 1~2분 걸릴 수 있습니다.

### 🧯 창이 바로 꺼지거나 오류가 날 때

오류가 나면 창이 닫히지 않고 원인을 보여준 뒤 엔터를 기다립니다.
같은 내용이 폴더의 **`오류기록.txt`** 에도 남으니, 그 파일을 보내주시면 됩니다.

가장 흔한 원인은 **`1_config.py`를 고칠 때 생긴 문법 오류**입니다.

| 증상 | 고치는 법 |
|---|---|
| 따옴표가 빠짐 | `API_KEY = "키"` — 키 앞뒤 모두 `"` |
| 둥근 따옴표 `“ ”` | 키보드의 일반 따옴표 `"` 로 바꾸기 (메신저·워드에서 복사하면 잘 생김) |
| 키가 두 줄로 나뉨 | 키를 한 줄로 붙여넣기 |
| 인코딩 | 메모장 → 다른 이름으로 저장 → 인코딩 **UTF-8** |

이 경우 화면에 **몇 번째 줄이 틀렸는지** 표시됩니다.

## 5. 제대로 적용됐는지 확인

봇이 켜지면 코인 목록에 이렇게 떠야 합니다:

```
SUI/USDT   UT:10,5   신호:즉시   EMA:34,55
```

- `UT:10,5` → UT Bot ATR 기간 5 ✅
- `신호:즉시` → 봉 마감 안 기다리고 즉시 진입 ✅

이렇게 안 뜨면 파일이 제대로 안 덮어써진 것입니다.

---

# 📁 파일 설명

## 자동매매 봇 (9개 — 전부 필요)

| 파일 | 역할 |
|---|---|
| `1_config.py` | **API 키 입력하는 곳** |
| `2_api.py` | 바이낸스 API 연결 |
| `3_indicators.py` | UT Bot, EMA, ADX 계산 |
| `4_bot.py` | 호환용 |
| `5_gui.py` | **핵심** — 화면 + 매매 로직 전부 |
| `6~8_gui_*.py` | 호환용 (내용 없음, 지우면 안 됨) |
| `9_main.py` | **실행 파일** |

## 별개 프로그램 (없어도 봇은 돌아감)

| 파일 | 실행법 | 용도 |
|---|---|---|
| `backtest.py` | `python backtest.py` | 백테스터 (혼자 돌아감) |
| `btc_1year.py` | `python btc_1year.py` | BTC 1년 백테스트 원클릭 |
| `goldfib_bot.py` | `python goldfib_bot.py` | 황금피보 전략 봇 (별개) |

---

# ⚙️ 현재 설정 요약

```
시간봉      1시간
진입금      50 USDT / 코인
레버리지    3배 (격리)
UT Bot      Key 10, ATR 5
EMA         34 / 55
진입 조건   UT 방향 + EMA 방향 (둘 다 맞아야)
진입 시점   즉시 (봉 마감 안 기다림)
익절        ADX≥21 → 1.5%  /  ADX<21 → 1.2%
손절        없음 (역신호 스위칭이 유일한 탈출구)
코인        BTC ETH BNB SOL XRP ADA DOGE TRX SUI LINK
```

---

# ⚠️ 시작 전 꼭 아셔야 할 것

### 1. 손절이 없습니다
역신호가 안 뜨면 강제청산까지 갑니다. 3배라서 가격이 33% 역행해야 터지지만,
**진입금 전액을 잃는 거래가 1년에 한 번쯤 나옵니다.**

### 2. 10코인은 같이 물립니다
상위 코인들은 서로 상관관계가 0.7 수준이라 BTC 빠지면 다 같이 빠집니다.
낙폭이 코인 1개의 **4배 이상**으로 겹칩니다.

**계좌 잔고를 증거금의 3~4배로 잡으세요.**
10코인 × 50 USDT = 증거금 500 → **계좌에 2,000 USDT 권장**

잔고가 부족하면 진입금을 50 → 15 USDT로 줄이세요.

### 3. 펀딩비가 나갑니다
8시간마다 냅니다. 수수료와 맞먹는 크기입니다.
로그에 실시간으로 표시되니 확인하세요.
비율이 0.05% 넘는 코인은 그 방향 포지션을 쉬는 게 낫습니다.

### 4. 반드시 테스트넷부터
최소 **2~3주** 테스트넷에서 돌려보세요. 확인할 것:
- 로그의 `⚠️ 리페인팅 발생` 비율 (5% 안팎이면 정상, 15% 넘으면 '봉 확정 후'로 변경)
- 실제 체결 품질과 펀딩비

### 5. 백테스트 수치는 시뮬레이션입니다
실제 바이낸스 데이터로 검증하지 못했습니다.
`python btc_1year.py` 를 돌려 실데이터로 직접 대조해보세요.
